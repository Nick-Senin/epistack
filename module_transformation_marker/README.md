# Module Transformation Marker

Модуль для семантического поиска и выделения **жирным шрифтом** (markdown) частей текста, которые относятся к указанным преобразованиям.

## 📋 Описание

TransformationMarker использует LLM для понимания семантической связи между текстом и списком преобразований. Модуль находит все части текста, которые связаны с указанными преобразованиями, и выделяет их markdown форматированием `**...**`.

### Особенности

- 🧠 **Семантический поиск** через LLM (не просто поиск ключевых слов)
- 📝 **Markdown форматирование** для выделения найденных частей
- 🎯 **ChainOfThought** рассуждения для лучшего понимания контекста
- 📊 **Оптимизация** с помощью GEPA/MIPRO/Bootstrap
- ✅ **Метрики качества** для оценки и оптимизации

## 🚀 Быстрый старт

### Базовое использование

```python
from module_transformation_marker import TransformationMarker, configure_module_llm

# Настройка LLM
configure_module_llm()

# Создание модуля
module = TransformationMarker()

# Использование
text = """
В процессе анализа данных сначала выполняется нормализация признаков,
чтобы привести их к единому масштабу. Затем применяется фильтрация
для удаления выбросов. На финальном этапе производится агрегация
результатов по категориям.
"""

result = module(
    text=text,
    transformations=["нормализация", "фильтрация", "агрегация"]
)

print(result.marked_text)
# Вывод: текст с выделенными **частями**, связанными с преобразованиями
```

### Демо-тест

```bash
python module_transformation_marker/demo_test.py
```

## 📦 Структура модуля

```
module_transformation_marker/
├── module.py          # Основной класс TransformationMarker
├── signatures.py      # DSPy сигнатура TransformationMarkerSignature
├── metrics.py         # Метрика оценки качества
├── optimize.py        # Функции оптимизации
├── demo_test.py       # Демо-тест с примерами
├── config.py          # Конфигурация LLM
├── __init__.py        # Экспорты модуля
└── README.md          # Документация
```

## 🔧 API

### TransformationMarker

Основной класс модуля.

```python
class TransformationMarker(dspy.Module):
    def forward(self, text, transformations):
        """
        Args:
            text (str): Исходный текст для обработки
            transformations (list[str]): Массив преобразований для поиска
            
        Returns:
            dspy.Prediction с полем marked_text - текст с выделениями
        """
```

### TransformationMarkerSignature

DSPy сигнатура для взаимодействия с LLM.

**Входные поля:**
- `text` - исходный текст
- `transformations: list[str]` - массив преобразований

**Выходное поле:**
- `marked_text` - текст с markdown выделениями `**...**`

### TransformationMarkerMetric

Метрика для оценки качества работы модуля.

Проверяет:
1. Корректность markdown синтаксиса
2. Наличие выделений
3. Сохранность исходного текста
4. Сравнение с эталоном (если доступен)

```python
metric = TransformationMarkerMetric(use_similarity=True)
score = metric(example, prediction)
```

## 📊 Оптимизация

### Подготовка датасета

```python
import dspy
from module_transformation_marker import optimize

# Создание датасета
# transformations передаются как массив строк
dataset = [
    dspy.Example(
        text="Данные проходят нормализацию и фильтрацию",
        transformations=["нормализация", "фильтрация"],
        expected_marked_text="Данные проходят **нормализацию** и **фильтрацию**"
    ).with_inputs("text", "transformations"),
    
    # ... больше примеров
]
```

### Запуск оптимизации

```python
# GEPA оптимизатор (рекомендуется)
optimized = optimize(
    dataset=dataset,
    max_metric_calls=50,
    optimizer_type='gepa'
)

# Сохранение оптимизированного модуля
optimized.save('module_transformation_marker/optimized_module.json')
```

### Типы оптимизаторов

#### GEPA (рекомендуется)
Эволюционная оптимизация промптов с рефлексией.

```python
optimize(dataset, optimizer_type='gepa', max_metric_calls=50)
```

#### MIPRO
Оптимизация промптов и примеров.

```python
optimize(dataset, optimizer_type='mipro')
```

#### Bootstrap
Генерация few-shot примеров.

```python
optimize(dataset, optimizer_type='bootstrap')
```

## 💡 Примеры использования

### Пример 1: Обработка данных

```python
text = """
Первым этапом обработки является нормализация данных.
Затем применяется фильтрация выбросов.
В конце выполняется агрегация результатов.
"""

result = module(
    text=text,
    transformations=["нормализация", "фильтрация", "агрегация"]
)
```

### Пример 2: Machine Learning пайплайн

```python
text = """
Модель обучается на тренировочном датасете в течение 100 эпох.
После обучения проводится валидация на отдельной выборке.
Затем модель тестируется на финальном тестовом датасете.
"""

result = module(
    text=text,
    transformations=["обучение", "валидация", "тестирование"]
)
```

### Пример 3: С несколькими преобразованиями

```python
# Преобразования передаются только в виде массива
result = module(
    text="...",
    transformations=["нормализация", "фильтрация", "агрегация", "масштабирование"]
)
```

## 🔍 Как это работает

1. **Входные данные**: Модуль получает текст и массив преобразований
2. **LLM анализ**: Модель анализирует текст и находит семантически связанные части с каждым преобразованием
3. **Выделение**: Найденные части оборачиваются в `**...**` для markdown
4. **Результат**: Возвращается текст с выделениями

## ⚙️ Конфигурация

### Настройка LLM

```python
from module_transformation_marker import configure_module_llm

# Использовать глобальный конфиг
configure_module_llm()

# Или специфичную модель
configure_module_llm(
    model="openai/gpt-4",
    temperature=0.7
)
```

### Переменные окружения

Создайте `.env` файл:

```bash
OPENROUTER_API_KEY=your_api_key
OPENROUTER_MODEL=openai/gpt-4
OPENROUTER_API_BASE=https://openrouter.ai/api/v1
```

## 📈 Метрики качества

Модуль оценивается по следующим критериям:

1. **Корректность синтаксиса** - правильное использование `**...**`
2. **Наличие выделений** - текст действительно выделен
3. **Сохранность текста** - исходный текст не изменён (кроме добавления `**`)
4. **Точность** - соответствие эталонному результату (если есть)

Итоговая оценка: `0.0` (плохо) до `1.0` (отлично)

## 🐛 Отладка

Если модуль не работает:

1. **Проверьте настройку LLM**
   ```python
   configure_module_llm()
   print(dspy.settings.lm)
   ```

2. **Запустите демо-тест**
   ```bash
   python module_transformation_marker/demo_test.py
   ```

3. **Проверьте формат датасета**
   - Каждый пример должен иметь `text` и `transformations`
   - Используйте `.with_inputs("text", "transformations")`

4. **Посмотрите рассуждения модели**
   ```python
   result = module(text="...", transformations=["..."])
   if hasattr(result, 'reasoning'):
       print(result.reasoning)
   ```

## 📚 Связанные модули

- [module_template](../module_template/) - шаблон для создания новых модулей
- [module_naming](../module_naming/) - пример оптимизированного модуля
- [epistack_data](../epistack_data/) - работа с датасетами

## 📄 Лицензия

Часть проекта epistack.

