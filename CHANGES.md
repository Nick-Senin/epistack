# Журнал изменений

## 2025-10-24 - Распределенная оптимизация модулей

### ➕ Добавлено

1. **Оптимизаторы в каждом модуле:**
   - `module_naming/optimize.py` - оптимизация RelationNamer
   - `module_extraction/optimize.py` - оптимизация RelationExtractor
   - `module_abstraction/optimize.py` - оптимизация NaiveATBAbstraction

2. **README в каждом модуле:**
   - Описание компонентов
   - Примеры оптимизации
   - Описание метрик

### ♻️ Изменено

1. **`optimize_modules.py`:**
   - Упрощен до импортов и вызовов
   - Теперь импортирует `optimize()` функции из модулей
   - Вся логика оптимизации перенесена в соответствующие модули

2. **`__init__.py` в модулях:**
   - Добавлен экспорт функции `optimize`
   - Доступна как `from epistack.module_naming import optimize`

### 💡 Преимущества

- Каждый модуль самодостаточен и имеет свой оптимизатор
- Легче поддерживать и модифицировать метрики
- Можно импортировать оптимизатор напрямую из модуля
- Улучшена модульность архитектуры

---

## 2025-10-24 - Преобразование в installable пакет

### ✅ Исправлено

1. **`__init__.py`** - Исправлены импорты:
   - Заменены абсолютные импорты на относительные (`.module_*`)
   - Удалены несуществующие модули (`evaluation`, `pipeline`)
   - Добавлены все доступные компоненты из существующих модулей
   - Исправлено: `from extraction import ...` → `from .module_extraction import ...`

2. **`main.py`** - Обновлен импорт:
   - Было: `from config import ...`, `from module_naming import ...`
   - Стало: `from epistack import configure_llm, RelationNamer`

3. **`module_abstraction/module.py`** - Исправлены внутренние импорты:
   - Было: `from utils.common_signatures import ...`
   - Стало: `from ..utils.common_signatures import ...`
   - Было: `from utils.helpers import ...`
   - Стало: `from ..utils.helpers import ...`

4. **`module_concretization/module.py`** - Исправлены внутренние импорты:
   - Было: `from utils.common_signatures import ...`
   - Стало: `from ..utils.common_signatures import ...`

5. **`README.md`** - Приведен в соответствие с реальной структурой:
   - Обновлена структура проекта (добавлены префиксы `module_`)
   - Удалены несуществующие модули
   - Добавлены инструкции по установке
   - Обновлены примеры использования

### ➕ Добавлено

1. **`setup.py`** - Классический файл установки:
   - Автоматическое обнаружение пакетов через `find_packages()`
   - Чтение зависимостей из `requirements.txt`
   - Метаданные пакета

2. **`pyproject.toml`** - Современная конфигурация пакета (PEP 518):
   - Декларативная конфигурация
   - Опциональные зависимости для разработки
   - Build system specification

3. **`INSTALL.md`** - Подробное руководство:
   - Инструкции по установке (editable и обычная)
   - Все варианты импорта модулей
   - Структура пакета
   - Список доступных компонентов

4. **`test_imports.py`** - Скрипт проверки:
   - Тестирует все импорты из основного пакета
   - Проверяет прямые импорты модулей
   - Удобная диагностика проблем

### 📋 Текущая структура модулей

```
epistack/
├── config/                      ✅
├── module_extraction/           ✅
├── module_naming/               ✅
├── module_abstraction/          ✅
├── module_concretization/       ✅
└── utils/                       ✅
```

### 🚀 Как использовать

**Установка:**
```bash
pip install -e .
```

**Проверка:**
```bash
python test_imports.py
```

**Использование:**
```python
# Вариант 1: Основной пакет
from epistack import RelationNamer, NaiveATBAbstraction

# Вариант 2: Конкретные модули
from epistack.module_naming import RelationNamer
from epistack.module_abstraction import NaiveATBAbstraction
```

### ⚠️ Breaking Changes

- Импорты без префикса `epistack.` больше не работают
- Удалены ссылки на несуществующие модули `evaluation` и `pipeline`
- `main.py` теперь требует установленный пакет для запуска

### 📝 Миграция кода

Если у вас есть код, использующий старые импорты:

**Было:**
```python
from module_naming import RelationNamer
from module_abstraction import NaiveATBAbstraction
```

**Стало:**
```python
from epistack.module_naming import RelationNamer
from epistack.module_abstraction import NaiveATBAbstraction

# Или
from epistack import RelationNamer, NaiveATBAbstraction
```

