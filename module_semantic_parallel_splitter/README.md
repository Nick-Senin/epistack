# Module Semantic Parallel Splitter

Модуль для семантической сегментации текста на логические смысловые блоки.

## 📋 Описание

Модуль разбивает длинный текст на семантически связанные сегменты, которые могут обрабатываться параллельно. Каждый сегмент представляет собой завершённую мысль или логический блок.

## 🚀 Использование

### Базовое использование

```python
from module_semantic_parallel_splitter import SemanticParallelSplitter, configure_module_llm

# Настройка LLM
configure_module_llm()

# Создание модуля
module = SemanticParallelSplitter()

# Сегментация текста
result = module(
    input_text="Ваш длинный текст...",
    max_segments=5
)

# Получение сегментов
print(result.segments)

# Статистика обработки (новое)
print(result.stats)
```

### С кастомными параметрами

```python
result = module(
    input_text="Ваш текст...",
    max_segments=10,
    min_segment_length=100,
    max_chunk_size=3000,
    num_threads=4,
    min_chunk_len=100,
    max_resplit_iters=3
)
```

## 📁 Структура

```
module_semantic_parallel_splitter/
├── module.py          # Основной DSPy модуль
├── signatures.py      # Сигнатуры для DSPy
├── metrics.py         # Метрики оценки качества
├── optimize.py        # Оптимизация модуля
├── demo_test.py       # Демо-тест
├── config.py          # Конфигурация LLM
├── __init__.py        # Экспорты
└── README.md          # Документация
```

## 🔧 Оптимизация

```python
from module_semantic_parallel_splitter import optimize

# Подготовка датасета
dataset = [
    dspy.Example(
        input_text="...",
        segments=["...", "...", "..."]
    )
    # ... больше примеров
]

# Оптимизация
optimized_module = optimize(
    dataset=dataset,
    max_metric_calls=50,
    optimizer_type='gepa'
)
```

## 📝 TODO

Для завершения реализации необходимо:

- [ ] Реализовать парсинг выходных сегментов в `metrics.py`
- [ ] Добавить расчёт покрытия текста в метрику
- [ ] Реализовать сохранение/загрузку оптимизированного модуля
- [ ] Добавить тестовые примеры в `demo_test.py`
- [ ] При необходимости настроить LLM as Judge для оценки качества

## 📚 Дополнительная информация

- [DSPy документация](https://dspy-docs.vercel.app/)
- [Шаблон модуля](../module_template/)
