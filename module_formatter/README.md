# Module Formatter

Модуль DSPy для форматирования текста с полным сохранением содержимого.

## 📋 Описание

`module_formatter` преобразует неструктурированный, плохо отформатированный текст в красиво оформленный markdown, сохраняя при этом **всё содержимое** без изменений. Модуль улучшает:

- **Структуру** - через заголовки и правильные абзацы
- **Читаемость** - через списки, выделение и переносы строк  
- **Оформление** - через исправление пробелов и пунктуации

### Ключевая особенность

**Полное сохранение содержимого** - модуль не добавляет и не удаляет информацию, только улучшает визуальное представление.

## 🚀 Быстрый старт

### Базовое использование

```python
from module_formatter import TextFormatter, configure_module_llm

# Настройка LLM
configure_module_llm()

# Создание модуля
formatter = TextFormatter()

# Форматирование текста
text = """Python это популярный язык программирования.Он используется для веб-разработки,анализа данных,машинного обучения.Основные преимущества простой синтаксис большое количество библиотек активное сообщество."""

result = formatter(text=text)
print(result.formatted_text)
```

### Запуск демо-теста

```bash
python module_formatter/demo_test.py
```

Демо-тест покажет форматирование на трёх примерах:
- Технический текст
- Инструкция
- Короткая заметка

## 📦 Структура модуля

```
module_formatter/
├── module.py          # TextFormatter - основной модуль
├── signatures.py      # FormatterSignature - задача форматирования
├── metrics.py         # FormatterMetric - оценка качества
├── optimize.py        # Оптимизация модуля
├── demo_test.py       # Демо-тест с примерами
├── config.py          # Конфигурация LLM
├── __init__.py        # Экспорты
└── README.md          # Документация
```

## 🎯 Как это работает

### 1. Сигнатура (FormatterSignature)

Определяет задачу форматирования с детальными инструкциями:
- Улучшить читаемость через абзацы и переносы
- Применить markdown (заголовки, списки, выделение)
- Исправить пробелы и пунктуацию
- **Критически важно**: сохранить всё содержимое

### 2. Модуль (TextFormatter)

Использует `ChainOfThought` для обдуманного форматирования:
```python
class TextFormatter(dspy.Module):
    def __init__(self):
        super().__init__()
        self.predictor = dspy.ChainOfThought(FormatterSignature)
    
    def forward(self, text):
        result = self.predictor(text=text)
        return result
```

### 3. Метрика (FormatterMetric)

Комплексная оценка включает:
- **Сохранение содержимого (50%)** - similarity между исходным и форматированным
- **LLM as Judge (30%)** - общая оценка качества
- **Markdown элементы (20%)** - наличие заголовков, списков, выделения

## 🔧 Оптимизация

### Подготовка датасета

Создайте датасет с примерами текстов для форматирования:

```python
import dspy

dataset = [
    dspy.Example(
        text="неотформатированный текст 1..."
    ).with_inputs('text'),
    dspy.Example(
        text="неотформатированный текст 2..."
    ).with_inputs('text'),
    # ... минимум 20-50 примеров
]
```

### Запуск оптимизации

```python
from module_formatter import optimize, configure_module_llm

# Настройка
configure_module_llm()

# Оптимизация с GEPA (рекомендуется)
optimized_formatter = optimize(
    dataset=dataset,
    max_metric_calls=50,
    optimizer_type='gepa'
)

# Сохранение
optimized_formatter.save('module_formatter/optimized_module.json')
```

### Типы оптимизаторов

#### GEPA (рекомендуется)
Эволюционная оптимизация промптов с рефлексией. Лучше всего подходит для задач форматирования, т.к. учится на детальном feedback.

```python
optimize(dataset, optimizer_type='gepa', max_metric_calls=50)
```

#### MIPRO
Оптимизация промптов и few-shot примеров.

```python
optimize(dataset, optimizer_type='mipro')
```

#### Bootstrap
Генерация few-shot примеров.

```python
optimize(dataset, optimizer_type='bootstrap')
```

## 📊 Метрика оценки

Метрика возвращает детальный feedback:

```
Оценка форматирования:
- Сохранение содержимого: 98%
- Markdown элементы: 75%
- LLM оценка: 85%
- Итоговая оценка: 91%

LLM обоснование: Текст отлично отформатирован с правильной структурой...

Рекомендации:
- ✅ Отличное форматирование с сохранением содержимого!
```

Если содержимое изменено, метрика выдаст критическое предупреждение:
```
- ⚠️ КРИТИЧНО: Содержимое изменено! Сохраняй всю информацию.
```

## 🎨 Примеры форматирования

### До

```
Python это популярный язык программирования созданный в 1991 году.Он используется для веб-разработки,анализа данных,машинного обучения.Основные преимущества простой синтаксис большое количество библиотек активное сообщество.
```

### После

```markdown
# Python

**Python** — популярный язык программирования, созданный в 1991 году.

## Области применения

Он используется для:
- Веб-разработки
- Анализа данных
- Машинного обучения

## Основные преимущества

- Простой синтаксис
- Большое количество библиотек
- Активное сообщество
```

## 🔍 Кастомизация

### Специфичная модель

```python
configure_module_llm(
    model="openai/gpt-4",
    temperature=0.3  # Низкая температура для стабильности
)
```

### Добавление контекста

Можно расширить сигнатуру для специфичных типов текста:

```python
class CustomFormatterSignature(dspy.Signature):
    """Форматирование технической документации..."""
    
    text = dspy.InputField(desc="Исходный текст")
    document_type = dspy.InputField(desc="Тип документа")
    formatted_text = dspy.OutputField(desc="Форматированный текст")
```

## ⚠️ Важные замечания

1. **Сохранение содержимого** - главный приоритет. Метрика строго оценивает это.
2. **Качество датасета** - для хорошей оптимизации нужны разнообразные примеры.
3. **Выбор модели** - используйте достаточно мощную модель (GPT-4, Claude и т.д.).
4. **Проверка результатов** - всегда проверяйте, что содержимое сохранено.

## 🐛 Отладка

### Модуль не форматирует должным образом

- Проверьте настройку LLM в `config.py`
- Убедитесь что модель достаточно мощная
- Попробуйте запустить оптимизацию с датасетом

### Содержимое изменяется

- Метрика должна выдавать предупреждения
- Используйте оптимизацию с GEPA для улучшения
- Добавьте больший вес на сохранение содержимого в метрике

### Ошибки импорта

```bash
# Убедитесь что вы в корневой директории проекта
cd /Users/admin/Documents/repos/epistack

# И запускаете как модуль
python module_formatter/demo_test.py
```

## 📚 Связанные модули

- [module_template](../module_template/) - Базовый шаблон
- [module_naming](../module_naming/) - Пример оптимизированного модуля
- [epistack_data](../epistack_data/) - Работа с датасетами

## 📄 Лицензия

Часть проекта epistack.
